/**
 * AlertDescription component
 * Framework: Pulsar
 */

import { cn } from '@pulsar-framework/design-tokens';
import type { IAlertDescriptionProps } from './alert-description.type';

/**
 * AlertDescription component
 * Description text for Alert component
 */
export const AlertDescription = ({
  children,
  className,
  ...rest
}: IAlertDescriptionProps): HTMLElement => {
  return (
    <div className={cn('text-sm', className)} {...rest}>
      {children}
    </div>
  );
};
