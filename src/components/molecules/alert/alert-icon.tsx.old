/**
 * AlertIcon component
 * Framework: Pulsar
 */

import { cn } from '@pulsar-framework/design-tokens';
import type { IAlertIconProps } from './alert-icon.type';

/**
 * AlertIcon component
 * Icon container for Alert component
 */
export const AlertIcon = ({ children, className, ...rest }: IAlertIconProps): HTMLElement => {
  return (
    <div className={cn('flex-shrink-0 w-5 h-5', className)} {...rest}>
      {children}
    </div>
  );
};
