/**
 * ModalBody component
 * Framework: Pulsar
 */

import { cn } from '@pulsar-framework/design-tokens';
import type { IModalBodyProps } from './modal-body.type';

/**
 * ModalBody component
 * Body section for Modal component
 */
export const ModalBody = ({ children, className, ...rest }: IModalBodyProps): HTMLElement => {
  return (
    <div className={cn('px-6 py-4', className)} {...rest}>
      {children}
    </div>
  );
};
