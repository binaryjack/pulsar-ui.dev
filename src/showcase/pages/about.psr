/**
 * Pulsar UI Showcase - About Page
 * Information about the framework and navigation test
 */

import { createSignal, useRouter } from '@pulsar-framework/pulsar.dev';

export component AboutPage() {
  const router = useRouter();
  const [showDetails, setShowDetails] = createSignal(false);
  
  const toggleDetails = () => {
    setShowDetails(!showDetails());
  };
  
  const goBack = () => {
    router.navigate('/');
  };
  
  return (
    <div style="padding: 20px;">
      <div style="margin-bottom: 30px;">
        <h2 style="font-size: 32px; margin-bottom: 10px; color: #3b82f6;">
          ℹ️ About Pulsar Framework
        </h2>
        <p style="font-size: 18px; color: #9ca3af; line-height: 1.6; margin-bottom: 20px;">
          Pulsar is a reactive UI framework built for performance and developer experience.
        </p>
      </div>
      
      <div style="background: #1a1a1a; padding: 30px; border-radius: 12px; margin-bottom: 20px;">
        <h3 style="font-size: 24px; margin-bottom: 20px; color: #10b981;">✨ Key Features</h3>
        <ul style="list-style: none; padding: 0; color: #d1d5db; font-size: 16px; line-height: 2;">
          <li>⚡ Signal-based reactivity</li>
          <li>🎯 TypeScript-first design</li>
          <li>🔄 Client-side routing with History API</li>
          <li>📦 Component-based architecture</li>
          <li>🎨 PSR (Pulsar Syntax) file format</li>
          <li>🚀 Optimized build pipeline with Vite</li>
        </ul>
        
        <button 
          onClick={toggleDetails}
          style="margin-top: 20px; padding: 12px 24px; font-size: 16px; background: #6b7280; 
                 border: none; color: white; border-radius: 6px; cursor: pointer; font-weight: bold;">
          {showDetails() ? '▼ Hide Details' : '▶ Show More Details'}
        </button>
        
        {showDetails() ? (
          <div style="margin-top: 20px; padding: 20px; background: #2a2a2a; border-radius: 8px; 
                      border-left: 4px solid #3b82f6;">
            <h4 style="font-size: 18px; margin-bottom: 10px; color: #3b82f6;">Navigation System</h4>
            <p style="color: #9ca3af; line-height: 1.6;">
              This page demonstrates Pulsar's built-in client-side routing capabilities. 
              The Router component uses the HTML5 History API to manage navigation without 
              full page reloads. Features include:
            </p>
            <ul style="color: #9ca3af; margin-top: 10px; margin-left: 20px;">
              <li>Declarative Route definitions</li>
              <li>useNavigate() and useRouter() hooks</li>
              <li>Path parameters and query strings</li>
              <li>Navigation guards</li>
            </ul>
          </div>
        ) : null}
      </div>
      
      <div style="background: #2a2a2a; padding: 20px; border-radius: 8px; margin-bottom: 20px;">
        <h3 style="font-size: 20px; margin-bottom: 15px; color: #f59e0b;">Current Route Info</h3>
        <p style="color: #9ca3af; margin-bottom: 10px;">
          <strong style="color: white;">Current Path:</strong> {router.location.pathname}
        </p>
        <p style="color: #9ca3af;">
          <strong style="color: white;">Search Params:</strong> {router.location.search || '(none)'}
        </p>
      </div>
      
      <button 
        onClick={goBack}
        style="padding: 14px 28px; font-size: 18px; background: #3b82f6; border: none; 
               color: white; border-radius: 8px; cursor: pointer; font-weight: bold; 
               box-shadow: 0 4px 6px rgba(59, 130, 246, 0.3);">
        ← Back to Home
      </button>
    </div>
  );
}
