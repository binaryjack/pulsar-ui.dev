// Test if statement pattern that causes the parsing issue
import { useState } from '@pulsar-framework/pulsar.dev';

component TestIfStatement() {
  const rootEl = document.querySelector('[data-test-id="test"]');
  
  if (rootEl) {
    const capture = captureRender(rootEl as HTMLElement, 'Test');
    console.log('Found element');
  }
  
  return <div>Test</div>;
}