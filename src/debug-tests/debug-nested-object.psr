/**
 * Minimal test for nested object literal issue
 */
import { useState } from '@pulsar-framework/pulsar.dev';

component TestNestedObject() {
  const [result, setResult] = useState('');
  
  // Simple object - should work
  const simple = test({ name: 'value' });
  
  // Nested object - might fail
  const nested = test({
    component: 'Test',
    config: {
      enabled: true,
      items: ['a', 'b', 'c']
    }
  });

  return (
    <div>Test</div>
  );
}

export { TestNestedObject };