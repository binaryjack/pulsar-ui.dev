/**
 * Debug test - type args + array literals (exact failing pattern)
 */

import { useState } from '@pulsar-framework/pulsar.dev';

component DebugExactFailure() {
  // This is the EXACT pattern from failing files
  const [items, setItems] = useState<string[]>(['Item 1', 'Item 2', 'Item 3']);

  return <div>Debug exact</div>;
}