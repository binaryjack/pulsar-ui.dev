/**
 * Debug test - isolate argument parsing issue
 */

import { useState } from '@pulsar-framework/pulsar.dev';

component DebugArgs() {
  // Test 1: Simple string argument
  const result1 = test("hello");
  
  // Test 2: Variable argument  
  const name = "world";
  const result2 = test(name);
  
  // Test 3: Zero arguments
  const result3 = test();

  return <div>Debug test</div>;
}