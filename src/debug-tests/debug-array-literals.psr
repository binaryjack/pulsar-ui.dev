/**
 * Debug test - array literals as arguments
 */

import { useState } from '@pulsar-framework/pulsar.dev';

component DebugArrayLiterals() {
  // Test 1: Array literal with strings
  const result1 = useState(['Item 1', 'Item 2', 'Item 3']);
  
  // Test 2: Array literal with numbers  
  const result2 = useState([1, 2, 3]);
  
  // Test 3: Empty array
  const result3 = useState([]);

  return <div>Debug arrays</div>;
}