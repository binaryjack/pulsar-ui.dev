// Test template literal with embedded conditional
import { useState } from '@pulsar-framework/pulsar.dev';

component TestTemplateConditional() {
  const [filterActive, setFilterActive] = useState<boolean>(false);
  
  return (
    <div>
      <button style={`background: ${filterActive() ? '#f56565' : '#4299e1'};`}>
        Template with conditional
      </button>
    </div>
  );
}