// Test just the template literal style issue
import { useState } from '@pulsar-framework/pulsar.dev';

component TestStyle() {
  const [filterActive, setFilterActive] = useState<boolean>(false);
  
  return (
    <div>
      <button
        style={`background: ${filterActive() ? '#f56565' : '#4299e1'};`}
      >
        Test
      </button>
    </div>
  );
}