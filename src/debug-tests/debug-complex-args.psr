/**
 * Debug test - complex argument patterns
 */

import { cn } from '@pulsar-framework/design-tokens';

component DebugComplexArgs() {
  const name = "John Doe";
  
  // Test 1: Multiple arguments
  const result1 = cn('class1', 'class2');
  
  // Test 2: Method chaining
  const result2 = name.trim();
  
  // Test 3: Method with arguments
  const result3 = name.substring(0, 2);
  
  // Test 4: Array access
  const parts = name.split(' ');
  const result4 = parts[0];

  return <div>Debug complex</div>;
}