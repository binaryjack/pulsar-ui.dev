# DEPRECATED — deploy has moved to the root monorepo:
# https://github.com/binaryjack/visual-schema-builder/blob/main/.github/workflows/deploy.yml
#
# This file is intentionally left disabled (no 'on:' trigger).
# The root workflow checks out visual-schema-builder directly
# so it does not need a cross-repo PAT_TOKEN.
name: Deploy to GitHub Pages (disabled — see root monorepo)
